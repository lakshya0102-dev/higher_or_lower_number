# Higher or Lower Number

Simple interactive number guessing game written in Python.
The compiler takes an random number between 1 and 100 and you need to guess it while the compiler tells you if the number is higher or lower than the actual number.

## Usage

Run the game:

```bash
python higher_or_lower_number.py
